/**
 * Насколько густо сыплются предметы: интервал между появлениями берётся
 * случайно из этого отрезка.
 *
 * Отдельным файлом, потому что читают его двое: планировщик спавна, который по
 * нему работает, и конфиг уровня, который его в себе носит.
 */
export interface SpawnPlan {
    readonly minInterval: number;
    readonly maxInterval: number;
}
