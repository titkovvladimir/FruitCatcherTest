/**
 * Состояние раунда.
 *
 * `idle` — раунд ещё не начинали: поле пустое, игрок выбирает сложность.
 * `finished` — раунд кончился и сам собой не начнётся: время вышло или
 * кончились жизни.
 */
export type LevelState = 'idle' | 'running' | 'paused' | 'finished';
